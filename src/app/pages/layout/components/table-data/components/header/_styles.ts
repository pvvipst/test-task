import { Box, styled } from '@mui/material'

const TABLE_DATA_HEADER_STYLES = {
  Wrapper: styled(Box)`
    display: flex;
    align-items: center;
    gap: 20px;
  `
}

export { TABLE_DATA_HEADER_STYLES }
