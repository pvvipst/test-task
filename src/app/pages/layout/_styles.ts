import { Box, styled } from '@mui/material'

const DEFAULT_PAGE_STYLES = {
  Wrapper: styled(Box)`
    padding: 100px 20px;
    display: grid;
    grid-template-columns: 270px 1fr;
    gap: 25px;
  `,
}

export { DEFAULT_PAGE_STYLES }
